import * as React from "react"
import { Text, View } from "react-native"

export default function DetailBookScreen() {
	return (
		<View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
			<Text>DetailBookScreen!</Text>
		</View>
	)
}
